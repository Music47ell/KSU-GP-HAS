name = "ksu-gp-has"
main = "dist/_worker.js"
compatibility_date = "2024-12-01"

[assets]
directory = "./dist"

# Environment vars are set as Cloudflare Secrets in the dashboard.
# They are NOT set here to avoid accidentally overriding secrets with empty values.
